import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'net.himakan.app.aiimage',
	appName: 'AI画像生成',
	bundledWebRuntime: false,
	webDir: 'build'
};

export default config;
